// Shared config for the SBC compare feature.
// CommonJS so it can be required from both Next.js pages (ESM) and
// build scripts like scripts/generate-sitemap.js (CJS).

const MAX_SBC_COMPARE = 3

module.exports = { MAX_SBC_COMPARE }
